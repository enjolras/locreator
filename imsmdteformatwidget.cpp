#include "imsmdteformatwidget.h"

imsmdTeFormatWidget::imsmdTeFormatWidget(FormatData *argFormatData, QWidget *parent) :
  //  langStringWidget(0, parent),
    formatData(argFormatData)
{

    //ui->langEdit->setVisible(FALSE);

    //ui->contentEdit->setText(formatData->format);

}
